# dependencies
node_modules/

# env (содержит DATABASE_URL, AUTH_SECRET)
.env.local
.env*.local

# Claude Code tooling (скилы/локальный конфиг — не код приложения)
.claude/

# logs
*.log
npm-debug.log*

# OS
.DS_Store
Thumbs.db

# user uploads (keep folder)
uploads/*
!uploads/.gitkeep

# local pglite / data
data/
*.bak
